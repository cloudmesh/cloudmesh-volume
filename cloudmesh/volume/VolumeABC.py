class volume()

            #volume create [name]
                  #[--size SIZE]
                  #[--type VOLUME-TYPE]
                  #[--image IMAGE | --snapshot SNAPSHOT | --source VOLUME]
                  #[--description DESCRIPTION]

            def create(name=name, size=size, voltype=voltype, image=image, snapshot=snapshot, source=source, description=description):
              raise not implemented error
